# simple kmeans for matlab and octave

Octave compatible kmeans function

The k-means clustering can get stuck in a local minimum. Therefore a set of different random starting cluster heads are chosen.
